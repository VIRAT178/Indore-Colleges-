services:
  - type: web
    name: indore-colleges-api
    runtime: node
    buildCommand: npm ci && npm run build:render
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000